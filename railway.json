{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "node": "18"
  },
  "deploy": {
    "startCommand": "cd backend && npm install && npm start"
  }
}
